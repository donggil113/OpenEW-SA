# Manuscript claim and numerical consistency audit

**PASS for bounded internal-review wording.**

The automated audit verifies 14 hash-linked evidence exports, 2,240 receiver–method–seed rows, 32 receivers, the five fixed seeds, finite metrics, no duplicate grid keys, 44 generated number macros, 13 resolved bibliography keys, eight figures, six main tables and all 160 receiver-seed lookup rows. Receiver means and T3A paired mean/positive count are reconciled from exported per-seed rows without rerunning models or resampling packets.

Primary performance numbers in abstract, results and conclusion come from generated LaTeX macros. Table/figure values are generated from the same source exports; fresh-clone regeneration leaves tracked files unchanged. The numerical matrix maps every result row to its source, analysis SHA and Git lineage. Structural constants, architectural numbers, seed values and analysis budgets are mapped separately in numerical_structural_traceability.md. Inference values are copied from the original frozen JSON, not newly estimated.

A line-level vocabulary/numeric-literal inventory is retained in the external manuscript audit JSON. Human contextual review covered every occurrence of state-of-the-art, generalization/general, robust, real-world, calibration, adaptation, temporal and related terms:
- “State of the art” appears only to deny broad superiority.
- “Generalization” names source-only evaluation/objectives; P2/T3A target access is explicit.
- “Robust” refers to prior method objectives or a limitation, not universal measured robustness.
- “Calibration” distinguishes receiver support, instrument/session semantics and probability calibration.
- Dynamic, hypergraph, temporal and neuro-symbolic claims are absent or explicitly unsupported.
- Synthetic validation is never described as real RF evidence.

Important retained caveats: later T3A inference follows earlier known WiSig results; LOSO training sets overlap; P2 is neutral, not proven equivalent; budget curves use a different common-query pool; the supervised oracle has labels; shuffled/mismatched donor domains differ; isolated cost components are unavailable. No favorable receiver, seed, budget, method or reference was selected during manuscript consolidation.

Regex scanning is an inventory, not proof of semantic correctness. Reviewer risk and novelty ledgers remain necessary. Final authorship, related OpenEW-SA publication metadata and institutional release approval remain unresolved.
