# Reviewer-remediation preregistration
Status: FROZEN BEFORE NEW ADDENDUM TARGET EVALUATION.
Evidence class: POST-HOC BASELINE-COMPLETENESS ADDENDUM. Designed after WiSig results and reviewer criticism; not independent confirmation or the original V2 inferential family.

## Scope and immutable reference
Base is PR89 merge 7b83dbcf25dc05f9130b75fdb92ce2d3ce225e92. PR80--89 scientific files and external artifacts remain read-only. Reuse exact V2 P0/P2/T3A and reference predictions after hash verification. No source retraining, P2 modification, split change, receiver/seed deletion, target selection, or Shen payload download.
Primary protocol: all 32 receiver_loso_00..31; seeds 829,1829,2829,3829,4829; exact split-local six-class mapping; source train/validation/test receiver roles unchanged.
128 target support packets are selected by frozen stable ID hash, without labels. Query is the exact disjoint V2 query. No query updates, query-query coupling, receiver value embeddings or day features.

## Frozen new conditions
SAR_GN: faithful SAR optimization on the existing GroupNorm network, with documented bounded-support access rather than the paper's online stream. P0 reset per receiver. Official parameter-name selection for affine GN parameters (no artificial BN); SAM radius .05, nonadaptive perturbation, SGD momentum .9, weight decay 0, one update per consecutive support minibatch of at most64 in frozen hash order. Learning rate .00025 for batch>=32; .00025/64*batch*2 below32, official GN rule. Reliability margin .4*log(C), entropy EMA .9, source-model recovery threshold .2 as in official code. No hyperparameter search. Empty reliable subset: skip that update, restore any SAM perturbation and record it; never propagate an undefined mean. Keep optimizer/model reset and retained EMA behavior equivalent to official forward wrapper. Query uses final adapted weights in eval mode and never triggers adaptation.
EMB_STD: explicit simple CONTROL, not a published named algorithm. Naked target z-scoring before an unmodified head destroys its source coordinate system; instead use diagonal moment transport z'=(z-mu_target)/max(sigma_target,1e-6)*max(sigma_source,1e-6)+mu_source. Source moments are population moments over all frozen source-training embeddings, label-free. Target moments use only support. Source head unchanged; no gradients, clipping, blend selection or query statistics. This is source-aligned embedding standardization; mean/std are per dimension, population variance (ddof0), float64 accumulation and float32 model input.
SUP_FT_FULL_128: LABEL-DEPENDENT ORACLE, not deployable or a mathematical ceiling. Copy P0; update all parameters using full-bank128 cross-entropy, AdamW zero weight decay. Candidate learning rates [1e-5,1e-4,5e-4] and steps [5,20]. Select maximum equal-receiver macro-F1 over the three frozen source-validation receivers using their own disjoint128 support/query simulation; ties follow listed order. Reset before each candidate/receiver. No query labels in adaptation; test support labels only. Existing head oracle remains a frozen reference.
SHOT is excluded as exact reproduction: official source recipe uses label smoothing, bottleneck/normalization and classifier parameterization absent from frozen P0. A new feature-only IM/pseudo-label variant would not reproduce that pipeline. Bounded support is not asserted intrinsically impossible; the source-recipe mismatch is the exclusion.
SHEN_GRL excluded: frozen compact256 WiFi IQ cannot reconstruct official long LoRa preamble/STFT geometry. No padding, concatenating unrelated packets or new raw dataset.
IN_RECEIVER_SUPERVISED excluded as a separate condition: full-network128 oracle already is labeled in-receiver adaptation. More labeled target data would require a new information regime/query protocol; no duplicate oracle called a real ceiling.

## Run grid and source-only gate
Per protocol/seed: three primary conditions above =480 new primary evaluations.
SAR_GN and EMB_STD budgets [0,16,32,64,128,256] use common queries after reserving256 exactly as prior budget study =1920 secondary evaluations. Budget128/common256 and primary128/exactV2queries are distinct records. Total planned new blind evaluations2400. Zero-support returns unchanged P0, not a new architecture.
No new P2/T3A budget runs: inherited frozen curves remain visible with lineage.
Source-only smoke is receiver_loso_00 seed829, all three methods using source-validation only. Technical failures may be repaired before target execution without new methods or choices. Model/analysis code hashes and exact grid must be committed before target execution; one GPU worker, checkpoint per protocol/seed, per-evaluation predictions. No target scores printed.
Calibration source predictions for P0,T3A,P2,SAR_GN,EMB_STD use exact frozen source-validation roles and support128; original T3A selected filter retained. Temperatures fit independently per protocol/seed/method with equal weight to each source-validation receiver. Fit log temperature bounded log(.05)..log(20) by scalar NLL minimization; tolerance1e-8, deterministic. Never target labels. Temperature changes scores only; original macro-F1 always reported.

## Metrics and inference
Receiver-equal macro-F1 is reference, accuracy and balanced accuracy secondary. Probability diagnostics:15 equal-width top-label ECE bins [0,1], adaptive15 equal-count bins with stable confidence order, multiclass NLL with probabilities clipped1e-12, multiclass Brier sum over classes (not divided by C), mean max confidence, mean correctness, confidence-minus-accuracy, predictive entropy. Reliability rows contain counts, mean confidence, accuracy and absolute/signed gaps. Empty bins explicitly empty. All32 receivers plotted; aggregate diagrams distinguish packet mass from receiver-equal conclusions.
Recompute old probability diagnostics from immutable predictions without replacing earlier outputs. Positive source-only temperature used on log probabilities; no claimed target-optimal calibration.
Five seeds averaged WITHIN receiver. New-method-minus-P0 and new-method-minus-T3A are descriptive/exploratory predefined contrasts; 10000 receiver bootstraps and100000 two-sided sign flips, RNG20260906. NO new Holm confirmatory family; report raw exploratory p-values and label multiplicity/unblinded design. No packet bootstrap. Standardized paired effect is receiver mean divided by sample SD; undefined if SD0.
Catastrophic adaptation is receiver/receiver-seed macro-F1 drop strictly greater than .05 versus matched P0. No bad-case deletion.
Compute: same GPU, synchronize timings, source-stat encoding, support encoding/adaptation, query prediction, peak allocated bytes, total/adapted parameters and gradient steps. Distinguish reused prior timings from new instrumentation. No FLOPs claim where not measured.

## One-time unblinding and reporting
Before joining any new target labels: all2400 expected records COMPLETE (or stop and document unresolved failures), unique config keys, finite normalized probabilities, correct exact query IDs, support disjointness, checkpoint/split/data/method hashes; clean committed analysis code; create-once prediction manifest; create-once unblinding manifest. Failed records cannot silently disappear.
After one-time unblinding no scientific redesign. Freeze analysis manifest before manuscript revision. All new claims carry POST-HOC labels. Publication readiness can remain CONDITIONAL/NOT_READY regardless of baseline outcome. External replication and physically acquired support realism remain unresolved.
