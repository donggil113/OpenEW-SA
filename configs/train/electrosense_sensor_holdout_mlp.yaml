artifact_dir: 'D:\openew_sa_data\processed\electrosense'
label_column: situation_label
output_dir: runs/electrosense_sensor_holdout_mlp
batch_size: 128
epochs: 10
lr: 0.001
split_strategy: domain_holdout
holdout_domains:
  - alcorcon1
  - bcn-L
  - Geneva
preprocessing:
  standardize: true
model:
  name: tabular_mlp
  kwargs:
    input_dim: 512
    num_classes: 6
