# Synthetic metadata contract fixture

These six rows are synthetic, target-independent acquisition records used only
to validate schema, provenance, relation, temporal-neighbor, hypergraph, and
dynamic-snapshot software contracts. Annotations are stored in a separate
file. No predictive model or scientific accuracy result is associated with
this fixture.

